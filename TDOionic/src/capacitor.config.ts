import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.Learn2Code.TutorDevOps',
  appName: 'TutorDevOps',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
